@page "/reservations"
@using MudBlazor
@using Heron.MudCalendar

<MudPopoverProvider />

<PageTitle>Reservaties</PageTitle>

<MudTabs Elevation="0" Centered="true">
    <MudTabPanel Text="Reserveer" data-testid="tab-reserve">
        <MudCalendar ShowDay="false" ShowWeek="false" ShowWorkWeek="false" Class="mt-8" data-testid="calendar-reserve"
            Items="AvailableDays" DateRangeChanged="OnDateRangeChanged">
            <MonthTemplate>
                <div class="d-flex gap-1">
                    <MudIcon Icon="@Icons.Material.Filled.Circle" Size="Size.Small"
                        Color="((ColoredCalendarItem)context).Color" />
                    <div>@context.Text</div>
                </div>
            </MonthTemplate>
        </MudCalendar>
    </MudTabPanel>
    <MudTabPanel Text="Uw Reservaties" data-testid="tab-your-reservations">
        <MudCalendar ShowDay="false" ShowWeek="false" ShowWorkWeek="false" Class="mt-8"
            data-testid="calendar-your-reservations" />
    </MudTabPanel>
</MudTabs>

@* Legende *@
<MudStack>
    <div class="d-flex gap-1">
        <MudIcon Icon="@Icons.Material.Filled.Circle" Size="Size.Small" Color="Color.Primary" />
        <div>Beschikbaar</div>
    </div>
    <div class="d-flex gap-1">
        <MudIcon Icon="@Icons.Material.Filled.Circle" Size="Size.Small" Color="Color.Warning" />
        <div>Volzet</div>
    </div>
</MudStack>