@using Rise.Shared.Notifications;
<MudStack Class="notifications-list" Spacing="2">
    @if (Notifications != null && Notifications.Any())
    {
        @foreach (var notification in Notifications)
        {
            <NotificationComponent Notification="notification"
                Class="@(SelectedNotification == notification ? "selected-notification" : " ")"
                OnClick="@(() => OnNotificationSelected.InvokeAsync(notification))" />
        }
    }
    else
    {
        <MudText Typo="Typo.body1" Color="Color.Inherit">No notifications</MudText>
    }
</MudStack>
